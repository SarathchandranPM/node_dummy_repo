// Local
const secrete = "SUPER SECRET";

// Shareable
const john = "John";
const peter = "Peter";

// console.log(module);

// module.exports = { john: john, peter: peter };

// Since we're following the ES6 syntax, we can simply write:

module.exports = { john, peter };

// as the name of property is same as its value
