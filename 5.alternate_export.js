// Export as you go:

module.exports.items = ["item-1", "item-2"];

const person = {
  name: "bob",
};

module.exports.singlePerson = person;

/* In this method, we set up a property/properties in the exports.

In the first example we set up a property named items and assigned it with an array ["item-1", "item-2"] and in the second example, we set up a property named person and assigned it with the person object. 
*/
